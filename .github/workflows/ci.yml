name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies (hub)
        working-directory: hub
        run: npm install

      - name: TypeScript check (hub)
        working-directory: hub
        run: npx tsc --noEmit

      - name: Build (hub)
        working-directory: hub
        run: npm run build

  plugin-lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install all workspaces
        run: npm install

      - name: TypeScript check (plugin)
        run: npx tsc --noEmit
        working-directory: plugin

      - name: Build (plugin)
        run: npm run build
        working-directory: plugin
